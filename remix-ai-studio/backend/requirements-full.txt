# Optional, heavy extras for full ML-based vocal/drum/bass/other stem separation.
# These pull in PyTorch (multi-GB download) and a pretrained Demucs model
# (also several hundred MB, downloaded on first use). Install only if you
# want higher-quality stems than the built-in center-channel heuristic.
#
#   pip install -r requirements.txt -r requirements-full.txt
#
# Then set REMIX_ENABLE_DEMUCS=true in your .env (see .env.example).
demucs==4.0.1
torch==2.3.1
torchaudio==2.3.1
